# Example environment variables for recipe_frontend
# Copy to .env and adjust as needed. Do NOT commit real secrets.

# PUBLIC_INTERFACE
# App URLs and API endpoints
REACT_APP_API_BASE=https://api.example.com
REACT_APP_BACKEND_URL=https://backend.example.com
REACT_APP_FRONTEND_URL=http://localhost:3000
REACT_APP_WS_URL=ws://localhost:3000

# PUBLIC_INTERFACE
# Runtime and build toggles
REACT_APP_NODE_ENV=production
REACT_APP_NEXT_TELEMETRY_DISABLED=1
REACT_APP_ENABLE_SOURCE_MAPS=false

# PUBLIC_INTERFACE
# Server binding and port (used by both dev server and static serve)
REACT_APP_PORT=3000
HOST=0.0.0.0

# PUBLIC_INTERFACE
# Proxy / logging / healthcheck
REACT_APP_TRUST_PROXY=true
REACT_APP_LOG_LEVEL=info
REACT_APP_HEALTHCHECK_PATH=/

# PUBLIC_INTERFACE
# Feature flags
REACT_APP_FEATURE_FLAGS={}
REACT_APP_EXPERIMENTS_ENABLED=false
