# PUBLIC_INTERFACE
# Example environment variables for recipe_frontend
# Copy to .env and adjust values as needed. The orchestrator may also inject these.

# App endpoints
REACT_APP_API_BASE=http://localhost:4000/api
REACT_APP_BACKEND_URL=http://localhost:4000
REACT_APP_FRONTEND_URL=http://localhost:3000
REACT_APP_WS_URL=ws://localhost:4000/ws

# Runtime behavior
REACT_APP_NODE_ENV=production
REACT_APP_NEXT_TELEMETRY_DISABLED=1
REACT_APP_ENABLE_SOURCE_MAPS=false
REACT_APP_FEATURE_FLAGS={}
REACT_APP_EXPERIMENTS_ENABLED=false

# Server bind and health
REACT_APP_PORT=3000
REACT_APP_TRUST_PROXY=true
REACT_APP_LOG_LEVEL=info
REACT_APP_HEALTHCHECK_PATH=/

# Note: In CI, prefer `npm run start:serve` for stability and low memory.
# `npm start` will auto-redirect to start:serve when CI=true.
