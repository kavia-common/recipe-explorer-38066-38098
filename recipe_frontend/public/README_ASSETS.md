This folder hosts static assets served at /assets/*.

At build time, ensure required images and CSS are available in public/assets. The app includes references like:
/assets/figma_image_30_811.png

If you add new assets under project root assets/figmaimages or recipe_frontend/assets, copy them into public/assets accordingly.
