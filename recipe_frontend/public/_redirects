# PUBLIC_INTERFACE
# SPA rewrite rules for static hosting and local `serve`:
# - Redirect all paths to index.html to support client-side routing (react-router)
# - Preserve 200 status for SPA behavior
/*  /index.html  200
